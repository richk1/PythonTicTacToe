An early attempt to write a GUI TicTacToe program in python.

Currently running using Python 3.5.2
It should also work with Python 2.7 if the tkinter import line is fixed as indicated in the source file.